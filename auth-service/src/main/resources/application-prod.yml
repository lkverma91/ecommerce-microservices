jwt:
  secret: ${JWT_SECRET}
  expiration-ms: ${JWT_EXPIRATION_MS:86400000}

frontend:
  base-url: ${FRONTEND_BASE_URL:http://frontend}
  auth-callback-path: ${FRONTEND_AUTH_CALLBACK_PATH:/auth/callback}
