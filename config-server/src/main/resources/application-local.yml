# ===========================================
# Config Server - LOCAL Profile
# ===========================================
# Uses embedded config-repo (classpath) - no external Git needed

server:
  port: 8888

spring:
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config-repo

logging:
  level:
    org.springframework.cloud.config: DEBUG
