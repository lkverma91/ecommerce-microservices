# ===========================================
# Config Server - Default Configuration
# ===========================================

spring:
  application:
    name: config-server
  # Config Server can use native (filesystem) or git backend
  cloud:
    config:
      server:
        # Native: serve from classpath or file path
        native:
          search-locations: classpath:/config-repo
        # Override with 'git' profile for production (Git backend)
        # git:
        #   uri: ${CONFIG_GIT_URI}
        #   default-label: main

# Eureka - register with service discovery
eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka/}
  instance:
    hostname: ${EUREKA_INSTANCE_HOSTNAME:localhost}

# Actuator
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: when_authorized
